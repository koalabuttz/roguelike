[package]
name = "roguelike-renderer3d"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
roguelike-core = { path = "../core", default-features = false }

[features]
default = ["std"]
std = ["roguelike-core/std"]
