[package]
name = "roguelike-saves"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
roguelike-core = { path = "../core", default-features = false, features = ["std"] }
